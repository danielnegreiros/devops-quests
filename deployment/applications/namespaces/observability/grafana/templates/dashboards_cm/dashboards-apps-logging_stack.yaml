apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-security-dashboards
  labels:
     grafana_dashboard: "1"
  annotations:
    grafana_dash_folder: "Security"
data:
{{ (.Files.Glob "dashboards/security/*.json").AsConfig | indent 2 }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-security-dashboards
  labels:
     grafana_dashboard: "1"
data:
{{ (.Files.Glob "dashboards/security/*.json").AsConfig | indent 2 }}
